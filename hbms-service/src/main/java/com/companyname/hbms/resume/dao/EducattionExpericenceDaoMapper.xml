<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.companyname.hbms.resume.dao.EducationExperienceDao">

  <insert id="insert" parameterType="com.companyname.hbms.resume.domain.EducationExperience">
    insert into EducationExperience(
      resumeId,
      school,
      startDate,
      endDate,
      degree
    )
    values (
      #{resumeId},
      #{school, jdbcType=VARCHAR},
      #{startDate,jdbcType=VARCHAR},
      #{endDate,jdbcType=VARCHAR},
      #{degree}
    )
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      select last_insert_id();
    </selectKey>
  </insert>

  <update id="update" parameterType="com.companyname.hbms.resume.domain.EducationExperience">
    update EducationExperience set
      school = #{school, jdbcType=VARCHAR},
      startDate = #{startDate,jdbcType=VARCHAR},
      endDate = #{endDate,jdbcType=VARCHAR},
      degree = #{degree}
    where
      id = #{id}
  </update>

  <delete id="deleteByIds" parameterType="java.lang.Long">
    delete from EducationExperience where id in
    <foreach collection="array" open="(" close=")" separator="," item="educationExperienceId">
      #{educationExperienceId}
    </foreach>
  </delete>
  
  <select id="findByBean"
          parameterType="com.companyname.hbms.resume.domain.EducationExperience"
          resultType="com.companyname.hbms.resume.domain.EducationExperience">
    select
      e.*
    from EducationExperience e
    where 1 = 1
    <if test="id != null">
      and id = #{id}
    </if>
    <if test="resumeId != null">
      and resumeId = #{resumeId}
    </if>
    <if test="school != null">
      and school = #{school}
    </if>
    <if test="startDate != null">
      and startDate = #{startDate}
    </if>
    <if test="endDate != null">
      and endDate = #{endDate}
    </if>
    <if test="degree != null">
      and degree = #{degree}
    </if>
  </select>


    <select id="findByIds"
            parameterType="com.companyname.hbms.resume.domain.EducationExperience"
            resultMap="domain.Resume">
        select * from EducationExperience where id in
        <foreach collection="array" open="(" close=")" separator="," item="educationExperienceId">
            #{educationExperienceId}
        </foreach>
    </select>

</mapper>