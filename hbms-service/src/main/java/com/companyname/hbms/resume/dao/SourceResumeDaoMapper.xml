<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.companyname.hbms.resume.dao.SourceResumeDao">

  <insert id="insert" parameterMap="domain.Resume">
    insert into SourceResume(
      name,
      binaryResume;
      textResume;
      yn,
      createTime,
      createUser,
      updateTime,
      updateUser
    ) values (
      #{name},
      #{binaryResume},
      #{textResume},
      1,
      now(),
      #{createUser},
      now(),
      #{updateUser}
    )
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      select last_insert_id();
    </selectKey>
  </insert>

  <update id="update" parameterMap="domain.SourceResume">
    update SourceResume set
      name = #{name},
      binaryResume = #{sex},
      textResume = #{telephone},
      updateTime = now(),
      updateUser = #{updateUser}
    where
      id = #{id}
  </update>

  <delete id="deleteByIds" parameterType="java.lang.Long">
    update SourceResume set yn = 0 where id in
    <foreach collection="array" open="(" close=")" separator="," item="sourceResumeId">
      #{sourceResumeId}
    </foreach>
  </delete>

  <select id="findByIds"
          parameterMap="domain.Resume"
          resultMap="domain.Resume">
    select * from SourceResume where id in
    <foreach collection="array" open="(" close=")" separator="," item="sourceResumeId">
      #{sourceResumeId}
    </foreach>
  </select>

</mapper>