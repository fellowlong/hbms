<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.companyname.hbms.candidate.dao.CandidateDao">

  <insert id="insert" parameterType="com.companyname.hbms.resume.domain.Certificate">
    insert into Certificate(
      resumeId,
      name,
      acquireDate
    )
    values (
      #{resumeId},
      #{name, jdbcType=VARCHAR},
      #{acquireDate,jdbcType=VARCHAR}
    )
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      select last_insert_id();
    </selectKey>
  </insert>

  <update id="update" parameterType="com.companyname.hbms.resume.domain.Certificate">
    update Certificate set
      name = #{name, jdbcType=VARCHAR},
      acquireDate = #{acquireDate,jdbcType=VARCHAR}
    where
      id = #{id}
  </update>

  <delete id="deleteByIds" parameterType="java.lang.Long">
    delete from Certificate where id in
    <foreach collection="array" open="(" close=")" separator="," item="certificateId">
      #{certificateId}
    </foreach>
  </delete>
  
  <select id="findByBean"
          parameterType="com.companyname.hbms.resume.domain.Certificate"
          resultType="com.companyname.hbms.resume.domain.Certificate">
    select
      *
    from Certificate
    where 1 = 1
    <if test="id != null">
      and id = #{id}
    </if>
    <if test="resumeId != null">
      and resumeId = #{resumeId}
    </if>
    <if test="name != null">
      and name = #{name}
    </if>
    <if test="acquireDate != null">
      and acquireDate = #{acquireDate}
    </if>
  </select>


    <select id="findByIds"
            parameterType="com.companyname.hbms.resume.domain.Certificate"
            resultType="com.companyname.hbms.resume.domain.Certificate">
        select * from Certificate where id in
        <foreach collection="array" open="(" close=")" separator="," item="certificateId">
            #{certificateId}
        </foreach>
    </select>

</mapper>