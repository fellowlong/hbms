<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.companyname.hbms.candidate.dao.ResumeDao">

  <insert id="insert" parameterType="com.companyname.hbms.resume.domain.Resume">
    insert into Resume(
      candidateId,
      name,
      keyword,
      attachUri,
      languageId,
      yn,
      createTime,
      createUser,
      updateTime,
      updateUser
    )
    values (
      #{candidateId},
      #{name, jdbcType=VARCHAR},
      #{keyword,jdbcType=VARCHAR},
      #{attachUri,jdbcType=VARCHAR},
      #{languageId},
      1,
      now(),
      #{createUser,jdbcType=VARCHAR},
      now(),
      #{updateUser,jdbcType=VARCHAR}
    )
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      select last_insert_id();
    </selectKey>
  </insert>

  <update id="update" parameterType="com.companyname.hbms.resume.domain.Resume">
    update Resume set
      candidateId = #{candidateId},
      name = #{name, jdbcType=VARCHAR},
      keyword = #{keyword,jdbcType=VARCHAR},
      attachUri = #{attachUri,jdbcType=VARCHAR},
      languageId = #{languageId},
      updateTime = now(),
      updateUser = #{updateUser,jdbcType=VARCHAR}
    where
      id = #{id}
  </update>

  <delete id="deleteByIds" parameterType="java.lang.Long">
    update Resume set yn = 0 where id in
    <foreach collection="array" open="(" close=")" separator="," item="resumeId">
      #{resumeId}
    </foreach>
  </delete>
  
  <select id="findByBean"
          parameterType="com.companyname.hbms.resume.domain.Resume"
          resultType="com.companyname.hbms.resume.domain.Resume">
    select
      r.*
    from Resume r
    where 1 = 1
    <if test="id != null">
      and id = #{id}
    </if>
    <if test="candidateId != null">
      and candidateId = #{candidateId}
    </if>
    <if test="name != null">
      and name = #{name}
    </if>
    <if test="keyword != null">
      and keyword = #{keyword}
    </if>
    <if test="languageId != null">
      and languageId = #{languageId}
    </if>
    <if test="yn != null">
      and yn = #{yn}
    </if>
  </select>


    <select id="findByIds"
            parameterType="com.companyname.hbms.resume.domain.Resume"
            resultMap="domain.Resume">
        select * from Resume where id in
        <foreach collection="array" open="(" close=")" separator="," item="resumeId">
            #{resumeId}
        </foreach>
    </select>

</mapper>