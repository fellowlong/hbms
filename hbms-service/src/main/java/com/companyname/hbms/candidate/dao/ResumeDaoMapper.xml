<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.companyname.hbms.candidate.dao.ResumeDao">

  <insert id="insert" parameterType="com.companyname.hbms.candidate.domain.Resume">
    insert into Resume(
      candidateId,
      name,
      keyword,
      path,
      languageId,
      type,
      yn
    )
    values (
      #{candidateId},
      #{name, jdbcType=VARCHAR},
      #{keyword,jdbcType=VARCHAR},
      #{path,jdbcType=VARCHAR},
      #{languageId},
      #{type},
      1
    )
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      select last_insert_id();
    </selectKey>
  </insert>

  <update id="update" parameterType="com.companyname.hbms.candidate.domain.Resume">
    update Resume set
      candidateId = #{candidateId},
      name = #{name, jdbcType=VARCHAR},
      keyword = #{keyword,jdbcType=VARCHAR},
      path = #{path,jdbcType=VARCHAR},
      languageId = #{languageId},
      type = #{type}
    where
      id = #{id}
  </update>

  <delete id="delete" parameterType="java.lang.Long">
    update Resume set yn = 0 where id = #{id}
  </delete>
  
  <select id="findByBean"
          parameterType="com.companyname.hbms.candidate.domain.Resume"
          resultType="com.companyname.hbms.candidate.domain.Resume">
    select * from Resume
    where 1 = 1
    <if test="id != null">
      and id = #{id}
    </if>
    <if test="candidateId != null">
      and candidateId = #{candidateId}
    </if>
    <if test="name != null">
      and name = #{name}
    </if>
    <if test="keyword != null">
      and keyword = #{keyword}
    </if>
    <if test="languageId != null">
      and languageId = #{languageId}
    </if>
    <if test="yn != null">
      and yn = #{yn}
    </if>
  </select>

</mapper>